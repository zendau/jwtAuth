import withAuth from "../HOC/withAuth";
import LoginLayout from "../layouts/auth.layout";

const Register = () => {
    return (
        <LoginLayout>
            <h1>Register</h1>
        </LoginLayout>
    );
};

export default Register;